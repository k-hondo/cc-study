---
paths:
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "**/*.spec.ts"
  - "**/*.spec.tsx"
---

# テストルール

- テスト名は「〜のとき、〜であること」の形式で記述する
- 実装の詳細ではなく振る舞いをテストする
- テスト間に依存を持たせず、任意の順序で実行可能にする
- 外部依存（API・DBなど）のみモックし、内部実装はモックしない
- 既存のテストは削除しない
- `afterEach` でテスト後のクリーンアップを行う
