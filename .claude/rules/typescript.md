---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

# TypeScript ルール

- `strict` モードを必ず有効にする
- `any` の使用は最小限に留める
- コンポーネントは Functional Component で記述する
- Props には必ず型定義を行う
- `export default` は避け、名前付きエクスポートを使用する
- `console.log` を本番コードに残さない
